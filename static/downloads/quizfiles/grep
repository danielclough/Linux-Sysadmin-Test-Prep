grep -c "[^a-zA-Z]" GPL-3 & How many characters in GPL-3 are not a letter?
grep -c "and$" GPL-3 & How many lines in GPL-3 contain "and" at the end of a line?
grep -c "^GPL" GPL-3 & How many lines in GPL-3 contain "GPL" at the beginning of a line?
grep -c -v "the" GPL-3 & How many lines in GPL-3 do not contain "the"?
grep -c "..cept" GPL-3 & How many lines in GPL-3 contain two characters and then the string "cept"?
grep "t[wo]o" GPL-3 | grep -c "[^m]ode" & How many lines in GPL-3 contain "too" or "two" and contain characters followed by "ode" except when that character is "m"?
grep -c "([A-Za-z ]*)" GPL-3 & How many lines in GPL-3 contain an opening and closing parenthesis with only letters and single spaces in between?
grep -c "^[A-Z].*\.$" GPL-3 & How many lines in GPL-3 begin with a capital letter and end with a period?
grep -c abeli* WORDS & How many words in WORDS include abel followed by zero or more i?
grep -c a[bc] GPL-3 & How many words in WORDS include a followed by b or c?
grep -c . GPL-3 & How many characters are in GPL-3?
grep -c [abc] WORDS & How many words in WORDS include either an a or a b or a c?
grep -c [a-z] WORDS & How many words in WORDS include lowercase letters?
grep -c "[[:digit:]]\." GPL-3 & How many numbers in GPL-3 are followed by a period?
